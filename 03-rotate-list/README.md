# Rotate List
## Description
Write a function named rotate that takes a list of integers and single integer k as its arguments and rotattes the list to the right by k steps. The number k may be positive or negative. If k > 0, you need to rotate the list k steps to the right, if k < 0, then you need to rotate the list k steps to the left.

Make sure that the signature of your function is as follows

"""
def rotate(nums, k) -> list:
    #your code here
"""python

Where nums is the list of itegers and k is the number of steps the list will be rotated by. Note that the order of the arguments matters.

To test your code, write two lines in the STDIN:
* The first line must contain the list nums as whitespare-separated integers
* The second line must contain a single number k.

### Notes
* Your function should return a list of integers
* The input list will always contain at least one integer